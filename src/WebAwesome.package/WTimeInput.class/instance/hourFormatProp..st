properties
hourFormatProp: propValue 
"'Whether the UI uses a 12-hour or 24-hour clock.  auto  follows the resolved locale.
type: WaTimeInputHourFormat
default: 'auto''"

self attributeAt: #'hourFormat' put: propValue