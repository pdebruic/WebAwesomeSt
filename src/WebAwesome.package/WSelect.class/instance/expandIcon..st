slots
expandIcon: slotRenderableObject 
"'  The icon to show when the control is expanded and collapsed. Rotates on open and close.'"

self slots at: #'expand-icon' put: slotRenderableObject.
slotRenderableObject slot: #'expand-icon'