properties
passwordToggleProp: propValue 
"'Adds a button to toggle the password's visibility. Only applies to password types.
type: boolean
default: false'"

self attributeAt: #'passwordToggle' put: propValue