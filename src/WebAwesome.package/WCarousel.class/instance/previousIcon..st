slots
previousIcon: slotRenderableObject 
"'  Optional previous icon to use instead of the default. Works best with '"

self slots at: #'previous-icon' put: slotRenderableObject.
slotRenderableObject slot: #'previous-icon'