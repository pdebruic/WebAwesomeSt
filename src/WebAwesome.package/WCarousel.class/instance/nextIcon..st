slots
nextIcon: slotRenderableObject 
"'  Optional next icon to use instead of the default. Works best with '"

self slots at: #'next-icon' put: slotRenderableObject.
slotRenderableObject slot: #'next-icon'