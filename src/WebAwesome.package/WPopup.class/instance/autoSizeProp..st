properties
autoSizeProp: propValue 
"'When set, this will cause the popup to automatically resize itself to prevent it from overflowing.
type: 'horizontal' | 'vertical' | 'both''"

self attributeAt: #'autoSize' put: propValue