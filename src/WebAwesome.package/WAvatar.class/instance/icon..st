slots
icon: slotRenderableObject 
"'  The default icon to use when no image or initials are present. Works best with '"

self slots at: #'icon' put: slotRenderableObject.
slotRenderableObject slot: #'icon'