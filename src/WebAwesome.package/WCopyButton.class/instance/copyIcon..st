slots
copyIcon: slotRenderableObject 
"'  The icon to show in the default copy state. Works best with '"

self slots at: #'copy-icon' put: slotRenderableObject.
slotRenderableObject slot: #'copy-icon'