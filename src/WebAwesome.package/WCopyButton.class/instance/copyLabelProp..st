properties
copyLabelProp: propValue 
"'A custom label to use as the accessible name and tooltip text in the default copy state.
type: string
default: '''"

self attributeAt: #'copyLabel' put: propValue