slots
navigationHeader: slotRenderableObject 
"'  The header for a navigation area. On mobile this will be the header for '"

self slots at: #'navigation-header' put: slotRenderableObject.
slotRenderableObject slot: #'navigation-header'