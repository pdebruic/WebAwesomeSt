slots
playIcon: slotRenderableObject 
"'  Optional play icon to use instead of the default. Works best with '"

self slots at: #'play-icon' put: slotRenderableObject.
slotRenderableObject slot: #'play-icon'