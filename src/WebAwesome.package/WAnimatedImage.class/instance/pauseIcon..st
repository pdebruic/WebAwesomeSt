slots
pauseIcon: slotRenderableObject 
"'  Optional pause icon to use instead of the default. Works best with '"

self slots at: #'pause-icon' put: slotRenderableObject.
slotRenderableObject slot: #'pause-icon'