slots
hint: slotRenderableObject 
"'  Text that describes how to use the checkbox group. Alternatively, you can use the '"

self slots at: #'hint' put: slotRenderableObject.
slotRenderableObject slot: #'hint'