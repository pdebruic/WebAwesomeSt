slots
label: slotRenderableObject 
"'  The checkbox group's label. Required for proper accessibility. Alternatively, you can use the '"

self slots at: #'label' put: slotRenderableObject.
slotRenderableObject slot: #'label'