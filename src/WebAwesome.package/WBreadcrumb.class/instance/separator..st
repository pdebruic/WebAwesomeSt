slots
separator: slotRenderableObject 
"'  The separator to use between breadcrumb items. Works best with '"

self slots at: #'separator' put: slotRenderableObject.
slotRenderableObject slot: #'separator'